# Dependencies
node_modules/

# Build output — compiled by Render during build step
dist/

# Local environment (set via Render env vars panel instead)
# .env  <-- intentionally NOT ignored here so Render can read it on first deploy
# But we keep the template committed

# Prisma
prisma/*.db
prisma/dev.db-journal

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS
.DS_Store
Thumbs.db
