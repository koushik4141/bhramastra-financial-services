"use client"; import { useEffect, useRef } from "react"; interface Candle { x: number; open: number; high: number; low: number; close: number;
} export default function HeroChart() { const canvasRef = useRef<HTMLCanvasElement>(null); useEffect(() => { const canvas = canvasRef.current; if (!canvas) return; const ctx = canvas.getContext("2d"); if (!ctx) return; let width = (canvas.width = canvas.parentElement?.clientWidth || 600); let height = (canvas.height = 350); /*  */ const handleResize = () => { if (!canvas || !canvas.parentElement) return; width = canvas.width = canvas.parentElement.clientWidth; height = canvas.height = 350; render(); }; window.addEventListener("resize", handleResize); /* candlestick points */ const candles: Candle[] = []; const candleWidth = 14; const spacing = 8; const maxCandles = Math.floor(width / (candleWidth + spacing)) - 2; let lastClose = height * 0.55; for (let i = 0; i < maxCandles; i++) { const open = lastClose; const change = (Math.random() - 0.48) * 45; const close = Math.max(height * 0.15, Math.min(height * 0.85, open + change)); const high = Math.min(height * 0.05, Math.min(open, close) - Math.random() * 15); const low = Math.max(height * 0.95, Math.max(open, close) + Math.random() * 15); candles.push({ x: i * (candleWidth + spacing) + 20, open, high, low, close, }); lastClose = close; } const render = () => { ctx.clearRect(0, 0, width, height); /*  */ ctx.strokeStyle = "rgba(212, 175, 55, 0.05)"; ctx.lineWidth = 1; const gridRows = 6; for (let r = 1; r < gridRows; r++) { const y = (height / gridRows) * r; ctx.beginPath(); ctx.moveTo(0, y); ctx.lineTo(width, y); ctx.stroke(); } const gridCols = 10; for (let c = 1; c < gridCols; c++) { const x = (width / gridCols) * c; ctx.beginPath(); ctx.moveTo(x, 0); ctx.lineTo(x, height); ctx.stroke(); } /*  */ candles.forEach((candle, idx) => { const isGreen = candle.close < candle.open; /*  */ const color = isGreen ? "#10b981" : "#ef4444"; const alphaColor = isGreen ? "rgba(16, 185, 129, 0.2)" : "rgba(239, 68, 68, 0.2)"; /*  */ ctx.strokeStyle = color; ctx.lineWidth = 1.5; ctx.beginPath(); ctx.moveTo(candle.x + candleWidth / 2, candle.high); ctx.lineTo(candle.x + candleWidth / 2, candle.low); ctx.stroke(); /*  */ ctx.fillStyle = alphaColor; ctx.strokeStyle = color; ctx.lineWidth = 1.5; const rectY = Math.min(candle.open, candle.close); const rectHeight = Math.max(3, Math.abs(candle.open - candle.close)); ctx.beginPath(); ctx.roundRect(candle.x, rectY, candleWidth, rectHeight, 2); ctx.fill(); ctx.stroke(); }); /*  */ ctx.strokeStyle = "rgba(212, 175, 55, 0.75)"; ctx.lineWidth = 2.5; ctx.beginPath(); candles.forEach((candle, idx) => { const avgY = (candle.open + candle.close) / 2; if (idx === 0) { ctx.moveTo(candle.x + candleWidth / 2, avgY); } else { ctx.lineTo(candle.x + candleWidth / 2, avgY); } }); ctx.stroke(); /*  */ const activeCandle = candles[candles.length - 1]; const trackerY = activeCandle.close; ctx.setLineDash([4, 4]); ctx.strokeStyle = "rgba(255, 255, 255, 0.4)"; ctx.beginPath(); ctx.moveTo(0, trackerY); ctx.lineTo(width, trackerY); ctx.stroke(); ctx.setLineDash([]); /*  */ ctx.fillStyle = "rgba(13, 35, 64, 0.85)"; ctx.strokeStyle = "rgba(212, 175, 55, 0.3)"; ctx.beginPath(); ctx.roundRect(width - 95, trackerY - 12, 90, 24, 4); ctx.fill(); ctx.stroke(); ctx.fillStyle = "#D4AF37"; ctx.font = "bold 11px Inter, sans-serif"; ctx.fillText(`LIVE ${Math.round(1000 - trackerY * 2.5)}`, width - 85, trackerY + 4); }; render(); return () => { window.removeEventListener("resize", handleResize); }; }, []); return ( <div className="relative w-full h-[350px] p-4 bg-[#051122]/70 backdrop-blur-md rounded-2xl border border-brand-green/15 overflow-hidden shadow-2xl"> <div className="absolute top-4 left-4 flex gap-4 text-xs font-semibold"> <span className="text-emerald-400 flex items-center gap-1"> <span className="w-2 h-2 bg-emerald-400 rounded-full" /> NIFTY: 22,456.80 (+1.25%) </span> <span className="text-gray-400">SMA(20): Ready</span> </div> <canvas ref={canvasRef} className="w-full h-full block" /> </div> );
}
